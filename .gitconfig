[alias]
	co = checkout
	st = status
	cg = config
	stats = shortlog
	cob = checkout -b
	cod = checkout develop
	com = commit -m
	cam = !git add -A && git commit -m
	po = push origin
	psup = push --set-upstream origin
	plo = pull origin
	plom = !git pull origin $(git_main_branch)
	fod = !git fetch && git merge origin/develop
	lgg = log --graph --color
	lo = log --oneline --decorate --color --graph
[user]
	name = John Oerter
	email = john.oerter@passportinc.com
[pull]
	rebase = false
[core]
  editor = nvim
[gitlab]
	user = joerter
[gitlab "pdev.io"]
	user = joerter
[gitlab "gitlab.pdev.io/api/v4"]
	user = joerter
[github]
	user = joerter
